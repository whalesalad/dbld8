!!! 5
%html
  %head
    %meta{ :content => "width=device-width", :name => "viewport" }
    %meta{ :name => "google-site-verification", :content => "6o8aTsEOlPquPfLBsCgWhmZCnEyfTp6Ngey0EI2ke0s" }
    %meta{ :name => 'apple-itunes-app', :content => "app-id=#{Rails.configuration.app_id}" }

    %title= full_title(yield(:title))
    
    = stylesheet_link_tag "application", :media => "all"
  
  %body{ :id => "#{params[:controller]}-section", :class => "#{body_class(yield(:extra_body_classes))}" }
  
    #home-wrapper
      %h1 DoubleDate
      %h2 Grab a friend • Go out • Have fun 

    #lower-glass
      = link_to "Download", itunes_url, :class => 'download-btn'

  <!-- #{cluster_host} -->

  = render 'layouts/footer'
